# Spec — InspectorRegistry

## Goal
Sustituir sidebar monolítico por contextual views.

## InspectorSection
`id, applies_to, priority, data_requirements, render_component, actions`.

## Built-ins
Overview, Evidence, Relations, Source, History, Intent, Runtime, Tests, UAT, Findings.

Plugin sections no escriben canonical facts directamente.
