# Spec — Search & ContextBundle

## Seed generators
Exact, lexical, current selection, recent change.

## Context item
`entity/evidence id, score, reason, path_from_seed, freshness, confidence, revision`.

## ContextBundle
`task, revision, items, budget, truncation, omitted_count, retrieval_strategy_versions`.

No hidden LLM fact puede entrar como Observed.
