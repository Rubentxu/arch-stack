# Spec — VisualRequest & VisualArtifact

## VisualRequest
`purpose, question, focus_ids, compare_ids, evidence_ids, lens_hint, overlay_hints, graph_revision`.

## VisualArtifact
`artifact_id, revision, lens_id, query/projection descriptor, focus, overlays, annotations, evidence refs, generated_by, rationale`.

No arbitrary agent HTML/JS como product contract.
