# Spec — ArchitectureStory

## Story
`id, title, goal, revision, steps, created_by`.

## Step
`lens, focus, selection, overlay, short_narrative, evidence_refs, optional_action`.

El usuario puede ramificar a una exploración y volver al mismo step.
