# TypeScript medium fixture — planned for M1

Placeholder for the M1 TypeScript medium repo (10–30k LoC). Same
constraints as `rust-tiny/`: cargo/npm metadata-ingestable, hand-labelled
gold set, SPDX license declaration.

`dependency-cruiser` is the planned extractor (the slow alternative to
`ctags` for dependency edges). Until then, this directory is empty and
the spike report measures only the 5-file canonical fixture + the
adversarial fixtures.
